properties:
  file.name:
    displayName: Page
views:
  - type: table
    name: Table
    filters:
      and:
        - note["topic@security"].contains(link("Security General"))
    sort:
      - property: file.name
        direction: DESC
